fastapi==0.115.0
uvicorn[standard]==0.30.6   # [standard] pulls uvloop + httptools for performance
gunicorn==23.0.0             # Process manager — uses UvicornWorker in production
pydantic==2.9.2
python-dotenv==1.0.1
httpx==0.27.2

# Azure SDK — Workload Identity authentication (no API keys)
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0

# Azure AI Search
azure-search-documents==11.6.0b9

# Azure OpenAI / AI Foundry
openai==1.57.0
